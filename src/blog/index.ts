import { content as morningRoutines } from './morning-routines-successful-people'

export const blogContent: { [slug: string]: { title: string; date: string; author: string; category: string; content: string } } = {
  'morning-routines-successful-people': morningRoutines,
}
